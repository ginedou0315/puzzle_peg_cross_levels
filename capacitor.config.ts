import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gnl.gdoubek.puzzlepegcross',
  appName: 'Puzzle_Peg_Cross',
  webDir: 'build'
};

export default config;
